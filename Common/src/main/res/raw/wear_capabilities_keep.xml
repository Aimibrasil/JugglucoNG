<?xml version="1.0" encoding="utf-8"?>
<!-- Play Services reads this array by name to advertise the app to the paired device; no code
     references it, so the resource shrinker drops it and the phone finds no capable nodes. -->
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@array/android_wear_capabilities" />
